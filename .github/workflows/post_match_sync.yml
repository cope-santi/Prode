name: Post Match Sync

on:
  schedule:
    # Estimated post-match syncs for the 2026 group-stage fixture.
    # Times are UTC and run about 2h10 after kickoff.
    - cron: "10 0 14,16,17,19,20,22,25 6 *"
    - cron: "10 1 15,18,24,26 6 *"
    - cron: "40 1 28 6 *"
    - cron: "10 2 21,23,27 6 *"
    - cron: "40 2 20 6 *"
    - cron: "10 3 13,14,16,17,19,22,25 6 *"
    - cron: "10 4 12,15,18,24,26,28 6 *"
    - cron: "10 5 20,23,27 6 *"
    - cron: "10 6 14,17,21 6 *"
    - cron: "10 18 15,18,21 6 *"
    - cron: "10 19 14,17,20,22,23 6 *"
    - cron: "10 21 11,12,13,15,16,18,19,21,24,26 6 *"
    - cron: "10 22 14,17,20,23,25 6 *"
    - cron: "10 23 22,27 6 *"
  workflow_dispatch: {}

concurrency:
  group: post-match-sync
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2

      - name: Setup Node
        uses: actions/setup-node@v6.4.0
        with:
          node-version: 24

      - name: Install deps
        working-directory: scripts/sync
        run: npm install

      - name: Run sync
        working-directory: scripts/sync
        env:
          FIREBASE_SERVICE_ACCOUNT_JSON: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}
          FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          THESPORTSDB_API_KEY: ${{ secrets.THESPORTSDB_API_KEY }}
          THESPORTSDB_LEAGUE_ID: ${{ secrets.THESPORTSDB_LEAGUE_ID }}
          THESPORTSDB_SEASON: ${{ secrets.THESPORTSDB_SEASON }}
          THESPORTSDB_ROUNDS: ${{ secrets.THESPORTSDB_ROUNDS }}
          SYNC_TEAM_LOGOS: ${{ secrets.SYNC_TEAM_LOGOS || secrets.SYNC_TEAM_LOGO || 'false' }}
          FORCE_TEAM_LOGOS: ${{ secrets.FORCE_TEAM_LOGOS }}
          TOURNAMENT_ID: ${{ secrets.TOURNAMENT_ID }}
          LOOKBACK_DAYS: ${{ secrets.LOOKBACK_DAYS }}
          POST_MATCH_SYNC_MINUTES: ${{ secrets.POST_MATCH_SYNC_MINUTES }}
          PROVIDER_CACHE_TTL_MS: ${{ secrets.PROVIDER_CACHE_TTL_MS }}
        run: node run.js
